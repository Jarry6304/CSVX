# Copy to .env and fill in. .env is gitignored.
# dotenvy loads this into the process environment at startup.
# These values override the corresponding fields in config.toml via figment's env provider.

SMI_DB_PASSWORD=<your_db_password>

# Optional: override tracing level (e.g. info, debug, trace)
# RUST_LOG=info
